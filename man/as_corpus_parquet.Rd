% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_corpus_parquet.R
\name{as_corpus_parquet}
\alias{as_corpus_parquet}
\title{Materialize a search parquet dataset as an openalexVectorise-shaped corpus}
\usage{
as_corpus_parquet(
  input,
  project_dir,
  corpus_name = "corpus",
  types = "search",
  abstract_from = "snippet",
  id_prefix = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{input}{Path to a search parquet dataset. Either an \code{endpoint_dir}
produced by \code{\link[=kagi_fetch]{kagi_fetch()}} (e.g. \verb{<project_folder>/search}, which contains
a \verb{parquet/} sub-folder), the \verb{parquet/} sub-folder itself, or a partition
path like \verb{<...>/parquet/query=<name>}.}

\item{project_dir}{openalexVectorise project root. The corpus is written
into \verb{<project_dir>/<corpus_name>/}.}

\item{corpus_name}{Folder name under \code{project_dir} (default \code{"corpus"},
matching openalexVectorise's default).}

\item{types}{Character vector of search result types to include
(default \code{"search"}). Set to multiple types (e.g. \code{c("search", "news")})
to include several. Set to \code{NULL} to include all types present.}

\item{abstract_from}{Field to use as the \code{abstract} column. Default
\code{"snippet"} (the searchResult.snippet field).}

\item{id_prefix}{Optional character. If non-NULL, the parquet rowwise \code{id}
is prefixed with this string (useful when stacking multiple corpora
in one project). Default \code{NULL} (use the existing \code{id} column unchanged).}

\item{overwrite}{Logical. If \code{TRUE}, an existing corpus dataset is deleted
before writing. Default \code{FALSE}.}
}
\value{
Invisibly, the normalized corpus path.
}
\description{
Read a Kagi search parquet dataset (written by \code{\link[=kagi_request_parquet]{kagi_request_parquet()}}
or \code{\link[=kagi_fetch]{kagi_fetch()}}) and project it to the three-column corpus schema
(\code{id}, \code{title}, \code{abstract}) consumed by \code{openalexVectorise::embed_corpus()}.
The output is a Hive-partitioned Arrow dataset under
\verb{<project_dir>/<corpus_name>/}.
}
\details{
Rows with empty \code{title} AND empty \code{abstract} are dropped, since
openalexVectorise's preprocessor would discard them anyway. The output
schema is exactly \code{id} (character), \code{title} (character), \code{abstract}
(character) — the minimum contract for
\code{openalexVectorise::embed_corpus()}. Any additional columns from the
source parquet are not propagated.
}
\examples{
\dontrun{
conn <- kagi_connection()
q <- kagi_query_search("biodiversity loss", workflow = "search", limit = 50)
parquet_path <- kagi_fetch(conn, q, project_folder = "kagi_demo")

as_corpus_parquet(
  input = parquet_path,
  project_dir = "/tmp/oavec_demo"
)
# → /tmp/oavec_demo/corpus/  is now ready for openalexVectorise::embed_corpus()
}

}
\seealso{
\code{\link[=kagi_query_search]{kagi_query_search()}}, \code{\link[=kagi_fetch]{kagi_fetch()}}, \code{\link[=kagi_request_parquet]{kagi_request_parquet()}}
}
