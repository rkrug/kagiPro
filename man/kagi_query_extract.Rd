% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kagi_query_extract.R
\name{kagi_query_extract}
\alias{kagi_query_extract}
\title{Build a Kagi Extract query}
\usage{
kagi_query_extract(pages, timeout = NULL, format = NULL, chunk_size = 10L)
}
\arguments{
\item{pages}{Character vector of HTTPS URLs. Required. Each URL is sent
through \verb{/extract} and converted to markdown.}

\item{timeout}{Optional numeric. Timeout in seconds for the extraction.}

\item{format}{Optional character. \code{"json"} (default) or \code{"markdown"}.}

\item{chunk_size}{Integer, default \code{10} (the API's per-request maximum).
Caller can lower this for finer-grained parallelism.}
}
\value{
A named list of \code{kagi_query_extract} objects, suitable for
\code{\link[=kagi_request]{kagi_request()}} or \code{\link[=kagi_fetch]{kagi_fetch()}}.
}
\description{
Construct one or more query objects of class \code{kagi_query_extract} for the
Kagi Extract API (\code{POST /extract}). Each query holds up to 10
HTTPS URLs. If more URLs are supplied, they are auto-chunked into multiple
query objects so the existing parallel dispatch in \code{\link[=kagi_request]{kagi_request()}} can
batch them.
}
\details{
The API requires HTTPS URLs and accepts 1-10 per request. This wrapper
validates the HTTPS scheme and chunks long input vectors automatically.
}
\examples{
\dontrun{
conn <- kagi_connection()
q <- kagi_query_extract(c(
  "https://example.com/article-1",
  "https://example.com/article-2"
))
kagi_fetch(conn, q, project_folder = "kagi_extract_demo")
}

}
\seealso{
\code{\link[=kagi_query_search]{kagi_query_search()}} for the \verb{/search} endpoint,
\code{\link[=kagi_connection]{kagi_connection()}},
\code{\link[=kagi_request]{kagi_request()}},
\code{\link[=kagi_fetch]{kagi_fetch()}}
}
