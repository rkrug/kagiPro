% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kagi_query_search.R
\name{kagi_query_search}
\alias{kagi_query_search}
\title{Build a Kagi Search query}
\usage{
kagi_query_search(
  query,
  workflow = c("search", "images", "videos", "news", "podcasts"),
  format = NULL,
  lens = NULL,
  lens_id = NULL,
  timeout = NULL,
  page = NULL,
  limit = NULL,
  filters = NULL,
  extract = NULL,
  safe_search = NULL,
  personalizations = NULL,
  expand = TRUE,
  open_in_browser = FALSE
)
}
\arguments{
\item{query}{Character vector of one or more search queries. Required.}

\item{workflow}{Character. One of \code{"search"}, \code{"images"}, \code{"videos"},
\code{"news"}, \code{"podcasts"}. Defaults to \code{"search"}.}

\item{format}{Optional character. \code{"json"} (default) or \code{"markdown"}
(experimental).}

\item{lens}{Optional named list describing an inline lens. See the OpenAPI
spec for the shape (\code{sites_included}, \code{sites_excluded},
\code{keywords_included}, \code{keywords_excluded}, \code{file_type}, \code{time_after},
\code{time_before}, \code{time_relative}, \code{search_region}).}

\item{lens_id}{Optional character. ID of a Kagi-side lens, or its full URL.
Mutually exclusive with \code{lens}.}

\item{timeout}{Optional numeric between 0.5 and 4.}

\item{page}{Optional integer between 1 and 10. Page number to request from
the API (body-paginated; the caller controls this directly).}

\item{limit}{Optional integer between 1 and 1024. Maximum number of results.}

\item{filters}{Optional named list (\code{region}, \code{after}, \code{before}).}

\item{extract}{Optional named list (\code{count}, \code{timeout}) requesting page
content extraction. Note: this incurs additional Extract-API cost.}

\item{safe_search}{Optional logical. Default \code{TRUE} (omits NSFW content).}

\item{personalizations}{Optional named list (\code{domains}, \code{regexes}).}

\item{expand}{Logical, default \code{TRUE}. If \code{TRUE} and \code{query} has multiple
terms, produce one query object per term.}

\item{open_in_browser}{Logical, default \code{FALSE}. If \code{TRUE}, each query is
also opened in the default browser via \code{\link[=open_search_query]{open_search_query()}}.}
}
\value{
A named list of \code{kagi_query_search} objects, suitable for
\code{\link[=kagi_request]{kagi_request()}} or \code{\link[=kagi_fetch]{kagi_fetch()}}.
}
\description{
Construct one or more query objects of class \code{kagi_query_search} for the
Kagi Search API (\code{POST /search}). Queries are typed JSON bodies that select
a workflow (\code{search}, \code{images}, \code{videos}, \code{news}, or \code{podcasts}) and may
carry filters, lens, extract, and personalization rules.
}
\details{
\strong{Default is intentionally neutral.} Calling \code{kagi_query_search("term")}
with no other arguments sends only \verb{\{"query": "term", "workflow": "search"\}}
to the API — no lens, no filters, no personalizations, no safe_search
override, no extract. Every shaping option is opt-in. This keeps default
results as objective and reproducible as the Kagi API itself allows.

If \code{query} is a character vector and \code{expand = TRUE}, a list of query objects
is returned (one per term) so the same \code{kagi_request()} parallel-dispatch
code path is used for batch fetches.

Validation matches the OpenAPI bounds: \code{timeout} in \verb{[0.5, 4]}, \code{page} in
\verb{[1, 10]}, \code{limit} in \verb{[1, 1024]}. The \code{lens} and \code{lens_id} arguments are
mutually exclusive. Other fields are passed through as-is and serialized to
the JSON request body when the request is performed.
}
\examples{
\dontrun{
conn <- kagi_connection()
q <- kagi_query_search(
  "biodiversity loss",
  workflow = "search",
  limit = 50,
  filters = list(region = "DE")
)
kagi_fetch(conn, q, project_folder = "kagi_demo")
}

}
\seealso{
\code{\link[=kagi_query_extract]{kagi_query_extract()}} for the \verb{/extract} endpoint,
\code{\link[=kagi_connection]{kagi_connection()}},
\code{\link[=kagi_request]{kagi_request()}},
\code{\link[=kagi_fetch]{kagi_fetch()}}
}
